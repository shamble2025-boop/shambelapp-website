# Backend Integration Variables
# The base URL of your existing REST API (used by Android App & Admin Panel)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# Secret token for On-Demand Revalidation (to sync data instantly when admin updates DB)
REVALIDATE_SECRET=your_super_secret_token_here